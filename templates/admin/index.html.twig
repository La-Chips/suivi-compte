{% extends 'base.html.twig' %}

{% block title %}Admin - Tech&Dev
{% endblock %}

{% block stylesheets %}

	<link rel="stylesheet" href="/css/admin.css">
{% endblock %}


{% block body %}

	{# {{form_start(add_statut)}}
											{{form_end(add_statut)}} #}

	<h1 class="text-center">Admin</h1>

	<a href="{{ path('app_status_index') }}" class="btn btn-secondary">Status</a>
	
	{# for ligne #}
	<a href="{{ path('ligne_index') }}" class="btn btn-secondary">Ligne</a>
	

	
	
	




{# 
	<div class="m-3">{{ render(path('user_index', {id: app.user.id})) }}</div>
	<div class="m-3">{{ render(path('extention_icon_index')) }}</div> #}


{% endblock %}
