{% extends 'base.html.twig' %}

{% block title %}ExtentionIcon{% endblock %}

{% block body %}
    <h1>ExtentionIcon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ extention_icon.id }}</td>
            </tr>
            <tr>
                <th>Extension</th>
                <td>{{ extention_icon.extension }}</td>
            </tr>
            <tr>
                <th>Icon_class</th>
                <td>{{ extention_icon.iconClass }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex">
        <button class="btn btn-primary m-2" onclick="window.location.href='/admin'">Retour à la liste</button>
        <button class="btn btn-info m-1" onclick="window.location.href='/extention/icon/{{ extention_icon.id }}/edit'">Modifier</button>

        {{ include('extention_icon/_delete_form.html.twig') }}
    </div>

{% endblock %}
