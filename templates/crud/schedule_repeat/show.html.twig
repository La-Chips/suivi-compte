{% extends 'base.html.twig' %}

{% block title %}ScheduleRepeat{% endblock %}

{% block body %}
    <h1>ScheduleRepeat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ schedule_repeat.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ schedule_repeat.label }}</td>
            </tr>
            <tr>
                <th>Day_duration</th>
                <td>{{ schedule_repeat.dayDuration }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_schedule_repeat_index') }}">back to list</a>

    <a href="{{ path('app_schedule_repeat_edit', {'id': schedule_repeat.id}) }}">edit</a>

    {{ include('crud/schedule_repeat/_delete_form.html.twig') }}
{% endblock %}
