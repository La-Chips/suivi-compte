{% extends 'base.html.twig' %}

{% block title %}BankAccount{% endblock %}

{% block body %}
    <h1>BankAccount</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bank_account.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ bank_account.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bank_account_index') }}">back to list</a>

    <a href="{{ path('app_bank_account_edit', {'id': bank_account.id}) }}">edit</a>

    {{ include('crud/bank_account/_delete_form.html.twig') }}
{% endblock %}
