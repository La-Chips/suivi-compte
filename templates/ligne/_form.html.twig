{{ form_start(form) }}
{{ form_widget(form) }}
<hr>
<div class="my-3">
<button class="btn btn-primary" role="button" data-bs-toggle="modal" data-bs-target="#createCategorie">Créer catégorie</button>
<button class="btn btn-success m-2" role="submit">{{ button_label|default('Sauvegarder') }}</button>
</div>

{{ form_end(form) }}