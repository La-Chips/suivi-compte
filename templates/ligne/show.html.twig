{% extends 'base.html.twig' %}

{% block title %}Ligne{% endblock %}

{% block body %}
    <h1>Ligne</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ ligne.date ? ligne.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ ligne.libelle }}</td>
            </tr>
            <tr>
                <th>Libelle_2</th>
                <td>{{ ligne.libelle2 }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ ligne.montant }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ ligne.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ligne_index') }}">back to list</a>

    <a href="{{ path('ligne_edit', {'id': ligne.id}) }}">edit</a>

    {{ include('ligne/_delete_form.html.twig') }}
{% endblock %}
