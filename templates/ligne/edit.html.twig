{% extends 'base.html.twig' %}

{% block title %}Edit Ligne
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Edit Ligne</h1>

		<div class="container">{{ include('ligne/_form.html.twig', {'button_label': 'Update'}) }}

			<div class="d-flex">
				<a href="{{ path('resume',{year : 'now' | date('Y')}) }}" class="btn btn-dark my-3">Retour</a>
				{{ include('ligne/_delete_form.html.twig') }}</div>


		</div>


	</div>

{% endblock %}
