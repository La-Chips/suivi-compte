<form method="post" action="{{ path('profesionnel_delete', {'id': profesionnel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ profesionnel.id) }}">
    <button class="btn">Delete</button>
</form>
